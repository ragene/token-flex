# DEPRECATED — CodeBuild project (token-flow-build) has been deleted.
# Deployment is now local-only. Use ecs-deploy.sh instead:
#
#   cd token-flow/aws && bash ecs-deploy.sh
#   cd token-flow/aws && bash ecs-deploy.sh --skip-build   # force-redeploy without rebuild
#
# This file is kept for reference only. Do not re-create the CodeBuild project.
