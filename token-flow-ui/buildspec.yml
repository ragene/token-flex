# DEPRECATED — CodeBuild project (token-flow-ui-build) has been deleted.
# Deployment is now local-only. Use deploy.sh instead:
#
#   cd token-flow/token-flow-ui && bash deploy.sh
#   cd token-flow/token-flow-ui && bash deploy.sh --skip-build
#
# This file is kept for reference only. Do not re-create the CodeBuild project.
